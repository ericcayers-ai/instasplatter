name: Feature request
description: Suggest an improvement or new capability
title: "[Feature]: "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        Please check [ROADMAP-V2.md](https://github.com/ericcayers-ai/instasplatter/blob/main/ROADMAP-V2.md), [ROADMAP.md](https://github.com/ericcayers-ai/instasplatter/blob/main/ROADMAP.md), and [docs/RESEARCH-STACK.md](https://github.com/ericcayers-ai/instasplatter/blob/main/docs/RESEARCH-STACK.md) before requesting large features.
  - type: textarea
    id: problem
    attributes:
      label: Problem / motivation
      description: What user problem does this solve?
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: Proposed solution
      description: What would you like InstaSplatter to do?
    validations:
      required: true
  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives considered
      description: Other approaches, existing settings, or external tools you already tried.
    validations:
      required: false
  - type: dropdown
    id: suite
    attributes:
      label: Suite
      options:
        - Reconstruction
        - Geospatial
        - Both suites / shared shell
        - Unsure
    validations:
      required: true
  - type: dropdown
    id: area
    attributes:
      label: Area
      options:
        - UI / UX
        - Reconstruction quality (SfM / dense init / training)
        - Geospatial (georeg / MapLibre / layers)
        - Flood (ANUGA / SWMM / preview / exports)
        - Performance / hardware presets
        - Export / formats
        - Engines / sidecars / Standard–Experimental gates
        - Docs / packaging
        - Other
    validations:
      required: true
  - type: checkboxes
    id: license
    attributes:
      label: License / dependency notes
      options:
        - label: Any new dependency or paper recipe I have in mind is Apache/MIT/BSD-friendly (or I am asking as research-only / Experimental)
          required: false
        - label: I searched existing issues and the roadmap for overlap
          required: true

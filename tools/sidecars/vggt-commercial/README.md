# VGGT-1B-Commercial (gated)

Standard when commercial terms are accepted (`ACCEPTED`) and weights are installed.
NC research checkpoints live under Experimental sidecars (`vggt-omega` / `vggt-research`).

```powershell
.\tools\sidecars\vggt-commercial\install.ps1
# place weights, provide run_vggt.py or upstream demo, then ACCEPTED
```

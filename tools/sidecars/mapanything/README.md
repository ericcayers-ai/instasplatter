# MapAnything (Apache) — Standard pose/depth hypothesis

Install the MapAnything launcher here (un.py / un.bat), accept weights,
then delete .stub.

Supports 	ask: "sfm" (COLMAP sparse under workspace) and 	ask: "densify".

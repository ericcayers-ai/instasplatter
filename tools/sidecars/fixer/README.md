# Fixer polish (NVIDIA Open Model — commercial OK when terms accepted)

Optional post-train polish. Host default ON when installed + ready.

```powershell
.\tools\sidecars\fixer\install.ps1
# provide run_fixer.py or upstream infer, weights, ACCEPTED
```

# Depth Anything 3 (Apache-2.0) — Standard densify candidate

Install weights + a un.py/un.bat launcher here, then delete .stub.

Protocol: JSON on stdin (	ask: "densify") → print absolute XYZRGB/Gaussian PLY path.

Replaces Depth Anything V2 as the preferred monocular depth densifier when present.

# Depth Anything V2 (Apache-2.0) — Standard densify (legacy)

Prefer `depth-anything-3` when available. Same protocol: densify → XYZRGB PLY.

```powershell
.\tools\sidecars\depth-anything-v2\install.ps1
pip install torch transformers opencv-python pillow
New-Item ACCEPTED under the engines copy
```
